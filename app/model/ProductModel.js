Ext.define('fr.ESIR.GreenVentory.model.ProductModel', {
    extend: 'Ext.data.Model',

    config: {
        fields: [
            'foodid','origin','picture','foodname','price','picture','seller_id','seller'
        ]
    }
});
